<div class="container">
<%= gmaps4rails(@json) %>

<h1>Clinicas</h1>

<table class="table table-striped">
  <tr>
    <th></th>
    <th>Nombre</th>
    <th>Direccion</th>
    
    
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clinics.each do |clinic| %>
  <tr>
    <td><%= image_tag clinic.picture_url(:thumb) if clinic.picture? %></td>
    <td><%= link_to clinic.name, clinic %></td>
    <td><%= link_to clinic.address, clinic %></td>
  
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Clinic', new_clinic_path %>
</div>